#Snek, by Jacob
#Based on Snake by Georgij

import random
import math as maths # <- important to rename it to maths, we're not american

'''
Game Settings
'''
mapSize = (11,13)

'''
Variable Definitions
'''

class BoardObj:
    # object that holds the values for the board
    def __ini__(self):
        # function to define the initial board
        
        map
        for i in range(0,11):
            for j in range(0,13):
                map[i][j] = 1
                print(map[i][j])
        print(map)
                
class SnakeObj:
    # object that stores and handles the snake values
    def __init__(self):
        a=1
                
class MainWindow:
    # object that handles the main gameplay loop
    def __init__(self):
        # initialises the game
        board = BoardObj()
        snake = SnakeObj()
        
    def mainLoop(self):
        # holds the main program loop
        a=1
# main program
if __name__=="__main__":
    main = MainWindow()
    
    
'''   
        map = []
        for i in range(0,11):
            for j in range(0,13):
                map[(i,j)].append(1)

        print(map)
        
        or 
        
        for i in range(0,11):
'''        